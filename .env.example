# BART Legacy API key — the public demo key below works for development.
# Register a project key at https://api.bart.gov/api/register.aspx before launch.
BART_API_KEY=MW9S-E7SL-26DU-VV8V

# Local services (docker compose up -d)
DATABASE_URL=postgres://baybound:baybound@localhost:5432/baybound
REDIS_URL=redis://localhost:6379

# Backend
PORT=4000
